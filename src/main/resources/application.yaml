server:
  port: 8080
  servlet:
    context-path: /identity

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/identity_service
    username: root
    password: APSKai.26072003
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true